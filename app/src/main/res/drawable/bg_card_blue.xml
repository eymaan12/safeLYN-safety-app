<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Gradient Background -->
    <gradient
        android:startColor="#DEECF8"
    android:endColor="#82A9CE"
    android:angle="90" />

    <!-- Optional Border -->
    <stroke
        android:width="2dp"
        android:color="@color/blue_dark" />

    <!-- Rounded Corners -->
    <corners android:radius="16dp" />

    <!-- Internal Padding -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>
